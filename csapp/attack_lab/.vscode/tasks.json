{
    "version": "2.0.0",
    "tasks": [
        {
		    "label": "make attack debug",
		    "type": "shell",
		    "command": "docker run --rm -t -v ${workspaceFolder}:/work --workdir /work bomb:v1 /bin/sh",
		    "problemMatcher": ["$gcc"],
		    "group": {
				"kind": "build",
				"isDefault": true
		    }
        }
    ]
}